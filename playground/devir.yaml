services:
  # Long-running web server (default type)
  web:
    icon: "🌐"
    dir: service1
    cmd: python3 -m http.server 4512
    port: 4512
    color: blue

  # Oneshot - runs once and exits
  setup:
    icon: "⚙️"
    type: oneshot
    dir: service1
    cmd: bash setup.sh
    color: yellow

  # Interval - runs periodically
  health:
    icon: "💓"
    type: interval
    interval: 5s
    dir: service1
    cmd: bash health.sh
    color: green

  # HTTP request service
  api-check:
    icon: "📡"
    type: http
    url: https://httpbin.org/get
    method: GET
    color: magenta

defaults:
  - setup
  - web
  - health
  - api-check
